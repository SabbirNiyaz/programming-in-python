# Expense Tracker App - Python Midterm Project
# Requirements file
# Install with: pip install -r requirements.txt

# tkinter is included with Python standard library (no pip install needed)
# No additional external packages required beyond the standard library.